# Generate 256-bit key (32 bytes)
# secret_key = secrets.token_hex(32)
# print(secret_key)  # hex string

# Or Base64 version

# secret_key_b64 = base64.b64encode(secrets.token_bytes(32)).decode()
# print(secret_key_b64)
