## Data Transformation Request

**Target Output Schema:**
```json
{{output_schema}}
```

**Source Input Data:**
```json
{{input_schema}}
```

**Task**: Generate a complete EJS template that transforms the input data to match the target schema exactly and upon render it should generate clean JSON.

**Required Template Features**:

You are an expert in EJS templating and I need a template that renders the variable inputData using EJS syntax (<%= %> for expressions, <% %> for logic), without comments, additional text, explanations, or assumptions about inputData being an array unless specified in the schema, and it should be a single line without new lines or slashes.